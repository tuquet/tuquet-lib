# @tuquet/eslint-config

> Shared ESLint 9 Flat Configuration for the @tuquet monorepo.

---

## 🛠 Usage

In your project's `eslint.config.mjs`:

```javascript
import config from '@tuquet/eslint-config';

export default config;
```
