# node-demo

> Playground application demonstrating how to consume `@tuquet/core` and `@tuquet/utils`.

---

## 🚀 Running the Demo

From the monorepo root:

```bash
pnpm --filter node-demo start
```

Or from within this directory:

```bash
pnpm start
```
